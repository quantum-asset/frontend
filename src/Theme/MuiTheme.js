import { createTheme } from "@mui/material/styles";

let MUImainTheme = createTheme({
  palette: {
    primary: {
      main: "#861616",
    },
    secondary: {
      main: "#ffffff",
    },
  },
});
export default MUImainTheme;
